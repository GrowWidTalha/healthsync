import { LucideIcon } from 'lucide-react'

export interface FeatureData {
  icon: LucideIcon
  title: string
  description: string
}

export interface TestimonialData {
    name: string
    role: string
    quote: string
  }
